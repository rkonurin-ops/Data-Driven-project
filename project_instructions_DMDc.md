# Инструкция к проекту: Улучшенный подход к реализации Dynamic Mode Decomposition with Control (DMDc)

**Источник:** Nedzhibov, G. *An Improved Approach for Implementing Dynamic Mode Decomposition with Control.* Computation, 2023, 11(10), 201. https://doi.org/10.3390/computation11100201

---

## 1. Обзор проекта

### 1.1 Цель

Реализовать на Python улучшенный алгоритм DMDc (Dynamic Mode Decomposition with Control), предложенный в статье Nedzhibov (2023), и оценить его эффективность на двух классических динамических системах:

- **Уравнения Лотки–Вольтерры** (Lotka–Volterra) — модель хищник–жертва;
- **Система Лоренца** (Lorenz system) — хаотическая динамика с внешним управлением.

### 1.2 Ожидаемые результаты

| Балл | Что требуется |
|------|--------------|
| 70% | Генерация данных, описание алгоритма DMDc |
| 80% | + реализация алгоритма, визуализация результатов |
| 90% | + метрики качества, сравнение со стандартным DMDc и PyDMD |
| Advanced | + глубокий анализ, сравнение с результатами статьи, аккуратное оформление |

### 1.3 Что сдаётся

1. **Jupyter Notebook** — весь код, эксперименты, визуализации с пояснительным текстом в ячейках Markdown;
2. **Письменный отчёт** (PDF или Word) — структурированный документ по разделам, описанным в §6;
3. **Презентация** (PowerPoint/PDF) — 10–15 слайдов для защиты по структуре из §7.

---

## 2. Математическая база

### 2.1 Постановка задачи

Рассматривается дискретная динамическая система с управлением:

$$\mathbf{x}_{k+1} = A\,\mathbf{x}_k + B\,\mathbf{u}_k, \quad k = 0, 1, \ldots, m-1,$$

где:
- $\mathbf{x}_k \in \mathbb{R}^n$ — вектор состояния системы в момент $k$;
- $\mathbf{u}_k \in \mathbb{R}^l$ — вектор управляющего воздействия;
- $A \in \mathbb{R}^{n \times n}$ — матрица динамики системы;
- $B \in \mathbb{R}^{n \times l}$ — матрица управления.

Задача: восстановить матрицы $A$ и $B$ по данным наблюдений $\{\mathbf{x}_k\}$ и $\{\mathbf{u}_k\}$.

Вводим матрицы снимков состояния и управления:

$$X = [\mathbf{x}_0, \mathbf{x}_1, \ldots, \mathbf{x}_{m-1}] \in \mathbb{R}^{n \times m},$$
$$X' = [\mathbf{x}_1, \mathbf{x}_2, \ldots, \mathbf{x}_{m}] \in \mathbb{R}^{n \times m},$$
$$\Upsilon = [\mathbf{u}_0, \mathbf{u}_1, \ldots, \mathbf{u}_{m-1}] \in \mathbb{R}^{l \times m}.$$

Тогда система записывается в матричном виде:

$$X' \approx A\,X + B\,\Upsilon = [A\ |\ B]\,\Omega, \quad \Omega = \begin{bmatrix} X \\ \Upsilon \end{bmatrix} \in \mathbb{R}^{(n+l) \times m}.$$

### 2.2 Стандартный алгоритм DMDc (Proctor et al., 2016)

**Шаг 1.** SVD расширенной матрицы:

$$\Omega = \tilde{U}\,\tilde{\Sigma}\,\tilde{V}^*, \quad \tilde{U} \in \mathbb{R}^{(n+l) \times p},$$

где $p$ — число оставляемых сингулярных значений (ранговое усечение).

**Шаг 2.** SVD матрицы $X'$:

$$X' = U\,\Sigma\,V^*, \quad U \in \mathbb{R}^{n \times r},$$

где $r \leq p$ — число мод в выходном пространстве.

**Шаг 3.** Разбиение $\tilde{U}$:

$$\tilde{U} = \begin{bmatrix} \tilde{U}_1 \\ \tilde{U}_2 \end{bmatrix}, \quad \tilde{U}_1 \in \mathbb{R}^{n \times p},\ \tilde{U}_2 \in \mathbb{R}^{l \times p}.$$

**Шаг 4.** Вычисление приближённых матриц $A$ и $B$:

$$\tilde{A} = U^*\,X'\,\tilde{V}\,\tilde{\Sigma}^{-1}\,\tilde{U}_1^*\,U,$$
$$\tilde{B} = U^*\,X'\,\tilde{V}\,\tilde{\Sigma}^{-1}\,\tilde{U}_2^*.$$

**Шаг 5.** Спектральная задача для «пониженного» оператора $\tilde{A}$:

$$\tilde{A}\,W = W\,\Lambda, \quad \Lambda = \mathrm{diag}(\lambda_1, \ldots, \lambda_r).$$

**Шаг 6.** Матрица динамических мод (DMD modes) в исходном пространстве:

$$\Phi = X'\,\tilde{V}\,\tilde{\Sigma}^{-1}\,\tilde{U}_1^*\,U\,W.$$

> **Ключевое замечание:** стандартный метод требует двух SVD (матриц $\Omega$ и $X'$), при этом SVD матрицы $\Omega$ производится над объектом размера $(n+l) \times m$, что дорого при больших $n$.

---

### 2.3 Улучшенный алгоритм DMDc (Nedzhibov, 2023)

Основная идея: **избежать SVD расширенной матрицы $\Omega$** — вместо этого использовать SVD матрицы меньшего размера и уменьшить число матричных умножений.

**Шаг 1.** SVD матрицы состояний $X$ (не $\Omega$!):

$$X = U_X\,\Sigma_X\,V_X^*, \quad U_X \in \mathbb{R}^{n \times r_1}.$$

**Шаг 2.** SVD матрицы управления $\Upsilon$:

$$\Upsilon = U_\Upsilon\,\Sigma_\Upsilon\,V_\Upsilon^*, \quad U_\Upsilon \in \mathbb{R}^{l \times r_2}.$$

**Шаг 3.** Вычисление приближённой матрицы $[A|B]$ через псевдообратные:

Оптимальное решение задачи $\min \|X' - [A|B]\,\Omega\|_F$ есть:

$$[A|B] = X'\,\Omega^+,$$

где $\Omega^+ = \Omega^*(\Omega\,\Omega^*)^{-1}$ — псевдообратная матрица.

Ключевой трюк: вместо SVD $\Omega$ авторы строят более компактное разложение через уже вычисленные $U_X, \Sigma_X, V_X$ и $U_\Upsilon, \Sigma_\Upsilon, V_\Upsilon$.

**Шаг 4.** Проекция в пространство редуцированного порядка через $U_X$:

$$\tilde{A} = U_X^*\,A\,U_X, \quad \tilde{B} = U_X^*\,B.$$

**Шаг 5.** Спектральный анализ редуцированного оператора $\tilde{A}$:

$$\tilde{A}\,W = W\,\Lambda.$$

**Шаг 6.** DMD-моды в исходном пространстве имеют более простую структуру:

$$\Phi = U_X\,W.$$

> **Сравнение с Proctor et al.:** в стандартном подходе $\Phi = X'\tilde{V}\tilde{\Sigma}^{-1}\tilde{U}_1^* U W$ — требует дополнительного умножения. В новом методе $\Phi = U_X W$ — структурно проще и дешевле.

**Вычислительное преимущество:**
- Нет SVD матрицы $(n+l) \times m$ — только две SVD матриц меньшего размера;
- Меньше матричных умножений при вычислении аппроксимационных матриц;
- Более простая структура матрицы динамических мод.

> **Важно:** изучите статью внимательно и воспроизведите точные формулы из Алгоритма 1 в разделе 3. Выше дана концептуальная схема — конкретные выражения уточните по тексту публикации.

---

## 3. Генерация данных

### 3.1 Система Лотки–Вольтерры

Уравнения хищник–жертва с управляющим воздействием $u(t)$:

$$\dot{x}_1 = \alpha\,x_1 - \beta\,x_1\,x_2 + u(t),$$
$$\dot{x}_2 = \delta\,x_1\,x_2 - \gamma\,x_2,$$

где $x_1$ — популяция жертвы, $x_2$ — популяция хищника.

**Параметры из статьи:**

$$\alpha = \tfrac{1}{2},\quad \beta = \tfrac{1}{40},\quad \gamma = \tfrac{1}{2},\quad \delta = \tfrac{1}{200}, \quad \mathbf{x}_0 = (60, 50)^T.$$

**Рекомендации по генерации:**

```python
import numpy as np
from scipy.integrate import solve_ivp

alpha, beta, gamma, delta = 0.5, 1/40, 0.5, 1/200
x0 = [60.0, 50.0]
t_span = (0, 30)
t_eval = np.linspace(0, 30, 300)

def lotka_volterra(t, x, u_func):
    x1, x2 = x
    u = u_func(t)
    dx1 = alpha * x1 - beta * x1 * x2 + u
    dx2 = delta * x1 * x2 - gamma * x2
    return [dx1, dx2]

# Пример управления: небольшая периодическая добавка
u_func = lambda t: 0.5 * np.sin(2 * np.pi * t / 5)

sol = solve_ivp(lotka_volterra, t_span, x0, t_eval=t_eval,
                args=(u_func,), method='RK45', rtol=1e-8)
```

**Формирование матриц снимков:**

```python
X  = sol.y[:, :-1]        # (2, m) — текущие состояния
Xp = sol.y[:, 1:]         # (2, m) — следующие состояния
U  = np.array([u_func(t) for t in t_eval[:-1]]).reshape(1, -1)  # (1, m)
```

### 3.2 Система Лоренца

Классическая хаотическая система с управлением:

$$\dot{x}_1 = \sigma\,(x_2 - x_1) + u_1(t),$$
$$\dot{x}_2 = x_1\,(\rho - x_3) - x_2,$$
$$\dot{x}_3 = x_1\,x_2 - \beta_L\,x_3 + u_2(t),$$

**Стандартные параметры:** $\sigma = 10$, $\rho = 28$, $\beta_L = 8/3$.

**Начальное условие:** $\mathbf{x}_0 = (1, 1, 1)^T$.

```python
sigma, rho, beta_L = 10.0, 28.0, 8/3

def lorenz(t, x, u_func):
    x1, x2, x3 = x
    u = u_func(t)   # вектор [u1, u2]
    dx1 = sigma * (x2 - x1) + u[0]
    dx2 = x1 * (rho - x3) - x2
    dx3 = x1 * x2 - beta_L * x3 + u[1]
    return [dx1, dx2, dx3]
```

**Управление для стабилизации хаоса:** выберите управление, которое уводит траекторию от хаотического аттрактора (например, к одной из двух неустойчивых неподвижных точек). Это основная задача в тесте Lorenz из статьи.

> **Примечание:** Используйте достаточно мелкий шаг интегрирования (`rtol=1e-10`, `atol=1e-12`) для корректного воспроизведения хаотической динамики.

---

## 4. Реализация алгоритмов

### 4.1 Структура кода

Организуйте код в виде классов или модулей:

```
project/
├── dmdc_standard.py    # Стандартный DMDc (Proctor et al., 2016)
├── dmdc_improved.py    # Улучшенный DMDc (Nedzhibov, 2023)
├── data_generation.py  # Генерация данных
├── metrics.py          # Метрики качества
├── visualization.py    # Графики
└── main_notebook.ipynb # Jupyter Notebook с экспериментами
```

### 4.2 Интерфейс классов

```python
class ImprovedDMDc:
    """
    Улучшенный алгоритм DMDc (Nedzhibov, 2023).
    
    Parameters
    ----------
    r1 : int — ранг для SVD матрицы X (число мод состояния)
    r2 : int — ранг для SVD матрицы Upsilon (число мод управления)
    """
    def fit(self, X: np.ndarray, Xp: np.ndarray, U: np.ndarray) -> None:
        """Обучение модели по данным снимков."""
        ...

    def predict(self, x0: np.ndarray, U_future: np.ndarray) -> np.ndarray:
        """Прогноз состояния на несколько шагов вперёд."""
        ...

    @property
    def A(self) -> np.ndarray:
        """Восстановленная матрица динамики."""
        ...

    @property
    def B(self) -> np.ndarray:
        """Восстановленная матрица управления."""
        ...

    @property
    def modes(self) -> np.ndarray:
        """Матрица динамических мод Phi."""
        ...

    @property
    def eigenvalues(self) -> np.ndarray:
        """Собственные значения Lambda."""
        ...
```

### 4.3 Выбор ранга (truncation)

Ранг $r$ выбирается на основе анализа сингулярных значений. Реализуйте оба подхода:

**Метод 1 — порог по относительной энергии:**

```python
def select_rank_energy(singular_values, threshold=0.9999):
    energy = np.cumsum(singular_values**2) / np.sum(singular_values**2)
    return np.searchsorted(energy, threshold) + 1
```

**Метод 2 — оптимальный порог Гавиша–Донохо (для зашумлённых данных):**

```python
def optimal_svht(singular_values, m, n):
    beta = min(m, n) / max(m, n)
    omega = 0.56 * beta**3 - 0.95 * beta**2 + 1.82 * beta + 1.43
    tau = omega * np.median(singular_values)
    return np.sum(singular_values > tau)
```

Визуализируйте график сингулярных значений с отмеченным порогом отсечения.

### 4.4 Baseline: стандартный DMDc

Реализуйте класс `StandardDMDc` согласно алгоритму Proctor et al. (2016). Следуйте той же структуре интерфейса, что и у `ImprovedDMDc`, — это упростит сравнение.

### 4.5 Baseline: PyDMD

```python
# pip install pydmd
from pydmd import DMDc as PyDMDc

pydmd_model = PyDMDc(svd_rank=r)
pydmd_model.fit(X, I=U)  # проверьте актуальный API в документации PyDMD
```

> **Важно:** Сравните не только точность прогноза, но и время выполнения. Используйте `timeit` или `time.perf_counter` для замеров.

---

## 5. Метрики качества и визуализация

### 5.1 Обязательные метрики

**Относительная ошибка реконструкции (Relative Error):**

$$RE(i, t_k) = \frac{|x_i^{\text{true}}(t_k) - x_i^{\text{pred}}(t_k)|}{|x_i^{\text{true}}(t_k)| + \varepsilon},$$

где $\varepsilon = 10^{-10}$ — регуляризатор (защита от деления на ноль).

**Средняя относительная ошибка (Mean Relative Error):**

$$MRE = \frac{1}{n \cdot m} \sum_{i=1}^n \sum_{k=1}^m RE(i, t_k).$$

**Ошибка восстановления матриц A и B:**

$$\varepsilon_A = \frac{\|A^{\text{true}} - \hat{A}\|_F}{\|A^{\text{true}}\|_F}, \quad \varepsilon_B = \frac{\|B^{\text{true}} - \hat{B}\|_F}{\|B^{\text{true}}\|_F}.$$

> Для нелинейных систем (Lotka–Volterra, Lorenz) точных $A$ и $B$ нет — оценивайте только качество прогноза траектории.

**Время выполнения:**

```python
import time

t0 = time.perf_counter()
model.fit(X, Xp, U)
t1 = time.perf_counter()
fit_time = t1 - t0
```

### 5.2 Дополнительные метрики (для оценки Advanced)

- **RMSE** (Root Mean Square Error) покомпонентно;
- **R² score** для каждой компоненты;
- **Спектральная ошибка** — расстояние между истинными и предсказанными собственными значениями (для линейных тестов);
- **Speedup** нового метода над стандартным DMDc.

### 5.3 Обязательные графики

Для каждой системы (Lotka–Volterra и Lorenz):

1. **Фазовый портрет** — истинная траектория vs прогноз DMDc;
2. **Компоненты состояния во времени** — $x_i(t)$ (true) vs $\hat{x}_i(t)$ (predicted), для каждой компоненты отдельно;
3. **Относительная ошибка во времени** — $RE(i, t)$ для каждой компоненты;
4. **График сингулярных значений** с порогом отсечения;
5. **Собственные значения на комплексной плоскости** — круг единичного радиуса + $\lambda_k$ трёх методов (стандартный, улучшенный, PyDMD);
6. **Сравнительная таблица метрик** (оформите как `pd.DataFrame`).

```python
# Пример: фазовый портрет Lotka-Volterra
import matplotlib.pyplot as plt

fig, axes = plt.subplots(1, 2, figsize=(14, 5))

# Фазовый портрет
axes[0].plot(X_true[0], X_true[1], 'k-', label='True', lw=2)
axes[0].plot(X_pred_improved[0], X_pred_improved[1], 'r--', label='Improved DMDc', lw=1.5)
axes[0].plot(X_pred_standard[0], X_pred_standard[1], 'b:', label='Standard DMDc', lw=1.5)
axes[0].set_xlabel('Prey $x_1$'); axes[0].set_ylabel('Predator $x_2$')
axes[0].legend(); axes[0].set_title('Phase Portrait: Lotka-Volterra')

# Компоненты во времени
axes[1].plot(t, X_true[0], 'k-', label='True $x_1$')
axes[1].plot(t, X_pred_improved[0], 'r--', label='Improved DMDc $x_1$')
axes[1].set_xlabel('Time'); axes[1].legend()
axes[1].set_title('State components over time')

plt.tight_layout()
plt.savefig('lotka_volterra_results.png', dpi=150)
plt.show()
```

---

## 6. Структура письменного отчёта

### Рекомендуемый объём: 10–20 страниц

```
1. Введение (1–2 стр.)
   - Мотивация: зачем нужен DMDc
   - Цели проекта
   - Структура отчёта

2. Теоретическая часть (3–5 стр.)
   2.1 Постановка задачи: динамическая система с управлением
   2.2 Стандартный алгоритм DMDc (Proctor et al., 2016)
   2.3 Улучшенный алгоритм DMDc (Nedzhibov, 2023)
   2.4 Сравнительный анализ двух подходов (вычислительная сложность,
       структура мод)

3. Описание данных (1–2 стр.)
   3.1 Система Лотки–Вольтерры: уравнения, параметры, режим управления
   3.2 Система Лоренца: уравнения, параметры, хаотический режим
   3.3 Формирование матриц снимков X, X', Υ

4. Реализация (1–2 стр.)
   4.1 Архитектура кода
   4.2 Выбор ранга (стратегия усечения SVD)
   4.3 Описание реализованных классов

5. Результаты экспериментов (3–5 стр.)
   5.1 Lotka-Volterra: фазовые портреты, компоненты во времени,
       относительная ошибка
   5.2 Lorenz: прогноз траектории, управление хаосом
   5.3 Сравнение трёх методов: таблица метрик, время выполнения

6. Обсуждение (1–2 стр.)
   6.1 Воспроизведение результатов из статьи
   6.2 Преимущества и ограничения нового метода
   6.3 Влияние выбора ранга на качество

7. Заключение (0.5 стр.)

8. Список литературы
   - Nedzhibov, 2023 (основная статья)
   - Proctor et al., 2016 (стандартный DMDc)
   - Schmid, 2010 (оригинальный DMD)
   - PyDMD документация
```

---

## 7. Структура презентации

### Рекомендуемый объём: 10–15 слайдов

| Слайд | Содержание |
|-------|-----------|
| 1 | Название, авторы, курс |
| 2 | Мотивация и цели проекта |
| 3 | Постановка задачи (формулы, смысл X, X', Υ) |
| 4 | Стандартный DMDc: схема алгоритма |
| 5 | Улучшенный DMDc: в чём новизна, ключевые отличия |
| 6 | Данные: Lotka–Volterra (уравнения + графики траекторий) |
| 7 | Данные: Lorenz (уравнения + аттрактор) |
| 8 | Результаты: Lotka–Volterra (графики сравнения) |
| 9 | Результаты: Lorenz (графики сравнения) |
| 10 | Сравнительная таблица метрик |
| 11 | Собственные значения (комплексная плоскость) |
| 12 | Выводы и обсуждение |
| 13 | (Опционально) Воспроизведение рисунков из статьи |
| 14 | Список литературы |

---

## 8. Критерии оценивания

### 70% — базовый уровень

- [ ] Данные сгенерированы корректно (обе системы, правильные параметры из статьи)
- [ ] Матрицы снимков $X$, $X'$, $\Upsilon$ сформированы верно
- [ ] В отчёте описан стандартный DMDc и улучшенный алгоритм (формулы, шаги)
- [ ] Код запускается без ошибок

### 80% — средний уровень (в дополнение к 70%)

- [ ] Реализован класс `ImprovedDMDc` с методами `fit` и `predict`
- [ ] Реализован `StandardDMDc` как baseline
- [ ] Построены фазовые портреты и графики компонент во времени для обеих систем
- [ ] Результаты для обеих систем (Lotka–Volterra и Lorenz) присутствуют в notebook

### 90% — продвинутый уровень (в дополнение к 80%)

- [ ] Вычислены метрики: RE(i,t), MRE, RMSE для всех трёх методов
- [ ] Подключён PyDMD как второй baseline и проведено сравнение
- [ ] Построена сравнительная таблица метрик и времени выполнения
- [ ] Есть график собственных значений на комплексной плоскости
- [ ] Результаты сопоставлены с рисунками и таблицами из статьи

### Advanced — отличный уровень (в дополнение к 90%)

- [ ] Воспроизведены конкретные рисунки из статьи (Fig. 1, Fig. 2 и др.) с теми же параметрами
- [ ] Проведён анализ влияния ранга $r$ на качество прогноза (ablation study)
- [ ] Обсуждены ограничения метода на нелинейных системах
- [ ] Отчёт оформлен структурно, логично, с правильными математическими обозначениями
- [ ] Код хорошо задокументирован (docstrings, комментарии)

---

## 9. Рекомендуемая литература и ресурсы

### Обязательная

1. **Nedzhibov, G.** An Improved Approach for Implementing Dynamic Mode Decomposition with Control. *Computation*, 2023, 11(10), 201. https://doi.org/10.3390/computation11100201

2. **Proctor, J. L., Brunton, S. L., & Kutz, J. N.** Dynamic Mode Decomposition with Control. *SIAM Journal on Applied Dynamical Systems*, 2016, 15(1), 142–161.

### Рекомендуемая

3. **Schmid, P. J.** Dynamic mode decomposition of numerical and experimental data. *Journal of Fluid Mechanics*, 2010, 656, 5–28.

4. **Brunton, S. L., & Kutz, J. N.** *Data-Driven Science and Engineering.* Cambridge University Press, 2022. (Chapters 7–8)

5. **Demo, N., Tezzele, M., & Rozza, G.** PyDMD: Python Dynamic Mode Decomposition. *Journal of Open Source Software*, 2018. https://pydmd.github.io/PyDMD/

### Полезные ссылки

- PyDMD документация: https://pydmd.github.io/PyDMD/
- Репозиторий PyDMD: https://github.com/PyDMD/PyDMD
- Brunton Lab tutorials: https://www.youtube.com/@eigensteve

---

## 10. Практические советы

**Работа с кодом:**
- Храните весь код в одном Jupyter Notebook с разбивкой на разделы через заголовки Markdown.
- После каждого блока кода давайте краткое пояснение в Markdown: что делает код и что означает результат.
- Фиксируйте `random_state` и начальные условия, чтобы результаты были воспроизводимы.

**Численная стабильность:**
- При вычислении псевдообратных используйте `rcond` / `rtol` в `np.linalg.lstsq` или `np.linalg.pinv`.
- При малых сингулярных значениях используйте регуляризацию: $\Sigma^{-1} \to \Sigma^{-1}$ с отсечением по порогу `rcond=1e-10`.

**Выбор ранга:**
- Не берите ранг слишком маленьким — потеряете точность. Не берите слишком большим — переобучитесь на шуме.
- Всегда показывайте график сингулярных значений и объясняйте, почему выбрали конкретный $r$.

**Воспроизведение статьи:**
- В статье Nedzhibov (2023) используются конкретные параметры для каждого теста. Используйте те же параметры для корректного сравнения.
- Сравнивайте ваши графики с Figure 1–2 (Lotka–Volterra) и Figure 3–4 (Lorenz) из статьи.

**Оформление:**
- Все оси на графиках должны быть подписаны с единицами.
- Все формулы в отчёте — с пояснением обозначений.
- Таблицы метрик — с округлением до 4–6 значащих цифр.
